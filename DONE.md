- [x] (**filter by price / era / series**) New Filter button on the left of the Play toolbar (opposite Keep Winner) opening `FilterModal` with three sections: a price range (min/max `market_price` in USD), an era multi-select (Vintage ≤2010 / Middle 2011–19 / Modern 2020+), and a searchable series multi-select. "Series" maps to the `cards.set` column (20 era values like Sword & Shield, Scarlet & Violet, Sun & Moon; `pack` is the finer 195-value expansion, not used here). `/api/comparison/next` accepts `minPrice`/`maxPrice`/`eras`/`series`: series and price are pushed into the Supabase query; era uses a DB `set IN (ERA_SETS)` pre-filter (so the contiguous random sample window is era-relevant) plus a precise JS release-year check (`matchesEras`) to trim boundary-spanning series (Promos span 1999–2026). Because a restrictive filter can leave a window with <2 eligible cards, the sample is retried with fresh offsets up to `SAMPLE_RETRIES` times before returning 409; the client shows a "No cards match these filters" message instead of crashing. Filters live in `ComparisonScreen` state + a `filtersRef` so the async fetches read current values, are serialized by `buildFilterQuery`, and applying re-fetches immediately. A dot badges the Filter button when any filter is active. Known limits: the series list is static (no `distinct_sets` RPC), `~10%` of cards have null `market_price` (excluded when a price bound is set), and filters reset on navigation. Files: `src/components/FilterModal.tsx`, `src/components/FilterButton.tsx`, `src/app/compare/ComparisonScreen.tsx`, `src/app/api/comparison/next/route.ts`.

- [x] (**no self-matchups**) A card must never be compared against itself. Both pair-builders in `/api/comparison/next` already exclude the first/winner card by `card_id` (`information_rich_pair` filters out `cardA`; `supply_winner_with_fresh_card` filters out the `winner`), so a literal id self-pair couldn't occur — but there was no guard against the source data carrying the same card under duplicate rows. Added a pool-level de-dup: the eligible rows are collected into a `Map` keyed by `card_id` (`eligibleById`, first row per id wins) before pairing, so each card appears at most once and the two selected entries are always distinct cards. Files: `src/app/api/comparison/next/route.ts`.

- [x] (**consistent card comparison**) A loaded comparison pair shouldn't disappear when the user leaves Play and comes back (e.g. to Rankings and back). Implemented in `ComparisonScreen`: the settled pair plus its streak (`{cards, streak, streakCardId}`) is written to `sessionStorage` under `pokemash:comparison` in an effect gated on `ready` (so only a fully-centered board is saved, never the transient `null`/mid-slide states). On mount, `readSavedComparison` restores that pair directly at center and `ready`, returning before the usual `loadNextPair` fetch; with nothing saved it falls back to fetching a fresh pair. sessionStorage (not localStorage) because this is a transient, per-tab concern, not durable progress. Restore runs in `useEffect` (not a `useState` lazy initializer) to avoid a hydration mismatch, since the component is server-rendered for the initial HTML. Files: `src/app/compare/ComparisonScreen.tsx`.

- [x] (**streak flame — make it a glow**) Winning-streak indicator (Keep Winner mode, tiers at 5/10/20 wins → dark red / blue / purple) on the held winner's card. Several attempts to render an actual flame were all rejected as ugly: (1) flickering box-shadow glow; (2) box-shadow ruffled by an SVG turbulence/displacement filter (`#flame-distort`); (3) teardrop `.flame-tongue` outlines rippled by that filter + opacity flicker; (4) outward-pointing SVG tongues rising/swaying via `scale`/`rotate` with a tip-fade mask + `drop-shadow` halo; (5) a full ring of upward-pointing SVG flame tongues (tall top crown, side climbers, bottom risers) filled by baked per-tier gradients, animating only `transform`/`opacity` with a static blur. FINAL (this entry): scrapped the flame entirely and went with a colored glow per the user's reference. `.flame` is a layer sitting just behind the card (`z-0`, `inset: -3px` so a thin colored backing shows at the border) with a solid `background: rgb(var(--flame-color))` fill plus a two-layer `box-shadow` halo glowing outward; only `opacity` animates (`flame-pulse`, compositor-cheap, keeps the edge from shifting). `--flame-color` is the tier triple from `flameColor(streak)`, which walks `STREAK_TIERS` (now four tiers: 5 red / 10 orange / 20 blue / 40 violet) and returns the highest reached. A minimal **legend** (`<ul>` of color-dot + `N+` rows, also driven by `STREAK_TIERS` as the single source) is pinned to the left edge, centered on the 25%-from-top line (75% up the card area). Removed all the tongue markup, gradient `<defs>`, `FLAME_TONGUES`/`FLAME_PATH`/`FLAME_GRADIENTS`, and every old flame keyframe/class. Streak-tracking logic (`streak`/`streakCardId`) unchanged. Files: `src/app/compare/ComparisonScreen.tsx`, `src/app/globals.css`.

- [x] (**rarity-restricted comparison pool**) Stop comparing boring cards by restricting the pool by rarity, with era-aware rules. Implemented in `/api/comparison/next` (app code, using the read-only key): exclude `Common`/`Uncommon`/`No Rarity`/`Double Rare`; keep plain `Rare` only for vintage sets (release year < 2023); keep all other rarities (holos, ex/GX, V/VMAX/VSTAR, Illustration/Special Illustration/Ultra/Secret/Hyper, Promo, Trainer Gallery, Mega, etc.). The route excludes the always-drop rarities server-side via a `not in` filter, samples a random ~1000-card window (to get variety past PostgREST's row cap), then drops modern plain `Rare` in JS because `release_date` is free text the DB filter can't compare by year. The user-chosen manual `Filter` button was removed beforehand. Equivalent SQL lives in `supabase/migrations/20260630_comparison_pool.sql` for a future move into the DB (tracked as an open TODO). Per-era mechanic rares are kept (regular GX in Sun & Moon, V/VMAX/VSTAR in Sword & Shield, EX in XY) — but not the regular ex of Scarlet & Violet / Mega Evolution, which is the already-dropped `Double Rare`. Energy cards are also excluded: matched by name (last word "Energy" after stripping `{symbols}`/"Prism Star"), which spares trainers like "Energy Retrieval"; this catches ~114 full-art/secret energies (Ultra Rare/Hyper Rare/ACE SPEC) that carry otherwise-kept rarities. Promos and plain `Rare` are both catch-all rarities (boring non-holos mixed with the odd buzzword chase card), so they're judged by card mechanic via `hasFeaturedMechanic` (name ends in GX/V/VMAX/VSTAR/ex/EX/LV.X/BREAK/Prime/LEGEND/Star) rather than rarity. A `Promo` is kept only with a mechanic; a `Rare` or `Rare Holo` is kept with a mechanic OR if genuinely vintage — released before Black & White (2011-03-01, i.e. HeartGold & SoulSilver and earlier), compared as a full date (`new Date(...)`) since the HGSS→BW boundary falls inside 2011. (`Rare Holo` follows the same vintage rule because a modern `Rare Holo` is a plain foil, e.g. Scizor 126/214 from 2018, while a vintage one is a chase card, e.g. Base Set Charizard.) This is safe because full-art cards always carry a distinct rarity (e.g. Reshiram 113/114 is `Ultra Rare`), so plain `Rare`/`Promo` never holds a full art. Net pool ~7688 eligible; drops modern non-buzzword `Rare` (e.g. Gardevoir 069/195, Silver Tempest 2022) while keeping vintage `Rare` and buzzword `Rare` (Deoxys ex, Umbreon Star).

- [x] (**exclude Item/Stadium trainers**) Trainer cards with subtype Item or Stadium are removed from the pool. The `cards` table has no card-type column, so they're matched by name against `src/lib/itemStadiumNames.ts` — a 573-name list (368 Item + 205 Stadium) generated from the Pokémon TCG API (`supertype:Trainer subtypes:Item/Stadium`). `normalizeName` in `api/comparison/next` (NFKD → drop non-ASCII → lowercase → punctuation-to-space → collapse) matches the list's normalization, so apostrophe/accent differences between sources don't matter. Supporters and Tools are kept. Removes the ~191 full-art/secret Items/Stadiums that were in the eligible pool (pool ~6885 → ~6694). The list is static and should be regenerated when new sets add Items/Stadiums.

- [x] (**add filters to comparison pool**) On left side of the `Play` screen, on the same level as the `Keep Winner` toggle, add a `Filter` button. Support the ability to filter by field names from the database. To start, only implement filtering on `cards.rarity`. When the user clicks the `Filter` button, a modal should popup that includes a search box titled `Rarity` above it. When the user clicks on the search box, the different values for `cards.rarity` column populate a scrollable dropdown menu. When the user types into the search box, only those values that contain the inputed string (when both are brought to lowercase) remain. There is a checkbox next to each of the values. When the user selects one of those values, it stays highlighted, and a small box with the name of the applied filter pops up next to the search box. In the code, this area will be called the `applied_filter area`. In this modal, there are three options to exit. First, the user can click the `Apply` button, which applies the filters and ensures that the next set of cards meets the filter criteria (that will required updating the `comparison/next` GET function). Next, there will be an `x` in the upper right corner that, when clicked, exits out without applying the filters. Or, the user can click anywhere outside of the modal, and the modal will close without applying filters (same function as hitting `x`).