- [x] (**rarity-restricted comparison pool**) Stop comparing boring cards by restricting the pool by rarity, with era-aware rules. Implemented in `/api/comparison/next` (app code, using the read-only key): exclude `Common`/`Uncommon`/`No Rarity`/`Double Rare`; keep plain `Rare` only for vintage sets (release year < 2023); keep all other rarities (holos, ex/GX, V/VMAX/VSTAR, Illustration/Special Illustration/Ultra/Secret/Hyper, Promo, Trainer Gallery, Mega, etc.). The route excludes the always-drop rarities server-side via a `not in` filter, samples a random ~1000-card window (to get variety past PostgREST's row cap), then drops modern plain `Rare` in JS because `release_date` is free text the DB filter can't compare by year. The user-chosen manual `Filter` button was removed beforehand. Equivalent SQL lives in `supabase/migrations/20260630_comparison_pool.sql` for a future move into the DB (tracked as an open TODO). Per-era mechanic rares are kept (regular GX in Sun & Moon, V/VMAX/VSTAR in Sword & Shield, EX in XY) — but not the regular ex of Scarlet & Violet / Mega Evolution, which is the already-dropped `Double Rare`. Energy cards are also excluded: matched by name (last word "Energy" after stripping `{symbols}`/"Prism Star"), which spares trainers like "Energy Retrieval"; this catches ~114 full-art/secret energies (Ultra Rare/Hyper Rare/ACE SPEC) that carry otherwise-kept rarities. Promos are held to the same bar as other cards: since every promo shares the single "Promo" rarity, they're judged by card mechanic instead — `isFeaturedPromo` keeps only featured chase cards (name ends in GX/V/VMAX/VSTAR/ex/EX/LV.X/BREAK/Prime/LEGEND/Star), dropping ~1783 plain-Pokemon promos and keeping ~836.

- [x] (**add filters to comparison pool**) On left side of the `Play` screen, on the same level as the `Keep Winner` toggle, add a `Filter` button. Support the ability to filter by field names from the database. To start, only implement filtering on `cards.rarity`. When the user clicks the `Filter` button, a modal should popup that includes a search box titled `Rarity` above it. When the user clicks on the search box, the different values for `cards.rarity` column populate a scrollable dropdown menu. When the user types into the search box, only those values that contain the inputed string (when both are brought to lowercase) remain. There is a checkbox next to each of the values. When the user selects one of those values, it stays highlighted, and a small box with the name of the applied filter pops up next to the search box. In the code, this area will be called the `applied_filter area`. In this modal, there are three options to exit. First, the user can click the `Apply` button, which applies the filters and ensures that the next set of cards meets the filter criteria (that will required updating the `comparison/next` GET function). Next, there will be an `x` in the upper right corner that, when clicked, exits out without applying the filters. Or, the user can click anywhere outside of the modal, and the modal will close without applying filters (same function as hitting `x`).