- [x] (**repo move to gspag77/CardMash**) Original ask: "I want my own version of the repo and call it CardMash… Expect liam to not respond until sunday so act accordingly with still launching tonight. Change any code so that everything points to my repo." Context: infra was consolidating under project-owned identities pre-launch (domain = user's Porkbun; Vercel = new account on cardmash.io@gmail.com; Liam's Hobby Vercel can't share access and Liam is unreachable until Sunday). DONE: created public `gspag77/CardMash` via the GitHub API (auth = the gspag77 credential git already held in the keychain; `gh` isn't installed — `git credential fill` + curl did the job), pushed FULL main history, updated the one in-code reference (privacy page's deletion-request issues link — the old repo was public and strangers file issues there, hence the new repo is public too), remotes re-pointed: `origin` → gspag77/CardMash, old repo kept as `liam` remote. The privacy fix was ALSO pushed to Liam's repo so the still-live old deploy pipeline serves the right link until the domain cutover. Liam has a pending collaborator invite (accepts Sunday; his catch-up command: `git remote set-url origin https://github.com/gspag77/CardMash.git`). Vercel import must now come from gspag77/CardMash — user's own GitHub owns it, so the Vercel GitHub-App install self-approves, nothing waits on Liam. Files: `src/app/privacy/page.tsx`.

- [x] (**launch prep: universal deep link + web analytics + Pitch Black import**) Original ask (pre-Reddit launch): "fix that universal link and add Pitch Black cards for launch. also add web analytics." THREE TASKS, three branches. (1) UNIVERSAL DEEP LINK: `/rankings` always seeded scope "mine", so no URL landed on the community leaderboard — the launch-critical linkable artifact. `rankings/page.tsx` now reads `?scope=universal` at mount (same one-shot seeding as `?player=`; other scopes stay URL-less deliberately — "mine" is the default, "friend" needs ?player=). Verified in driven Chrome: fresh anonymous context + `?scope=universal` → Universal chip active, 100 community cards render; control without the param lands on empty Mine. `cardmash.io/rankings?scope=universal` is now the postable link. (2) WEB ANALYTICS: `@vercel/analytics` installed, `<Analytics />` in the root layout (cookieless page views + referrers; no-ops in dev). Privacy page gained an Analytics section disclosing exactly that (cookieless, anonymous, never linked to rankings) — the Cookies section's "no tracking or analytics cookies" stays TRUE because Vercel Web Analytics stores nothing in the browser (daily-rotating server-side hash). STILL MANUAL: enable Web Analytics on the project in the Vercel dashboard, else the beacon 404s (harmless). Launch links should carry `?ref=` tags (reddit-tcg, hn, riib…), which show up as query params in the dashboard. (3) PITCH BLACK (PBL, Mega Evolution set 5, rel. Jul 17 2026 — the Mega Darkrai set): pkmncards had NOTHING the day before release (set page + guessed image URLs all 404), so the import came from tcgcsv (TCGplayer's daily catalog mirror, the same source as the id backfill): new `scripts/import-set.ts <tcgcsvGroupId> "<pack>" "<set>" "<date>"` fetches a group's numbered singles and prints a pack+collector_number-guarded INSERT (source-abstract columns only; run via MCP/SQL editor — the publishable key can't write cards). It fills `tcgplayer_product_id` at insert time (known for free from the source), so refresh-prices ran immediately with NO separate id backfill; image_url points at TCGplayer's aspect-preserved `_in_1000x1000` scans (718×1000 real card scans day-of; the `{id}.jpg` bare URL 403s, `_200w`/`_400w` also exist). Set code PBL read off the card scan itself ("PBL EN" bottom-left). 120 singles inserted; rarity vocabulary matched ours exactly (verified against existing Mega Evolution rows before insert). Era plumbing (SERIES filter option, ERA_SETS modern window) already covered set="Mega Evolution" — zero code needed there. Exclusion list: pokemontcg.io lags new sets, so the 6 new Item/Stadium/Tool names (antique armor/skull fossil, backtrack badge, dark bell, fossil quarry, tremendous bomb) were hand-added to `excludedTrainerNames.ts` from tcgcsv's "Card Type" field (which cleanly distinguishes Item/Tool/Stadium/Supporter — better provenance than the API for day-one sets); header comment says to fold them into the next regeneration. Stamp: 23,018 scanned → 6,732 eligible = old 6,702 + exactly 30 (PBL #085–120 minus the 6 Ultra Rare Item/Tool reprints; Supporters kept). Art backfill: 30/30 uploaded → art_url stamped → temp anon INSERT policy dropped. NOTE for next run: the script's HEAD existence check (`fetch(objectUrl, {method:"HEAD"})`) returned non-ok for ~400 EXISTING objects (mass 409 "Duplicate" upload rejections + some 429 too_many_connections; echoes the known HEAD-vs-GET storage artifact in LEARN.md) — harmless (uploads bounce off existing objects, "4 uploaded" did land) but noisy and bandwidth-wasteful; consider switching the check to a ranged GET. Prices: full refresh ran (22,574 staged, NM verification 2,019/2,019) — all 120 PBL cards carry presale market prices (SIR Mega Darkrai $396, MHR $365). Verified end-to-end: `/api/comparison/next?series=Mega Evolution` deals through the art proxy, and the SIR Mega Darkrai serves as a 650×905 WebP at `/art/{id}.webp`. Files: `src/app/rankings/page.tsx`, `src/app/layout.tsx`, `src/app/privacy/page.tsx`, `scripts/import-set.ts` (new), `src/lib/excludedTrainerNames.ts`, package.json (@vercel/analytics).

- [x] (**colocate Vercel functions with Supabase**) Original ask: "i still run into slow loading of cards. It is usually fast, but slow enough that it bothers me, and will to users." MEASURED, not guessed: prod `/api/comparison/next` warm TTFB 0.7–2.2s; `/art/…` Vercel-edge MISS 0.4–1.2s (HIT 0.1s) — all TTFB, not transfer, with `x-envoy-upstream-service-time: 4` showing 4ms of actual DB work. Two false leads eliminated first: the Next image optimizer was already off (`unoptimized: true`), and a `cache-control: no-cache` header on Supabase storage objects turned out to be a HEAD-request artifact — GETs serve `public, max-age=31536000` and Cloudflare caches them fine (all 6,846 objects' metadata verified correct via SQL, so the planned metadata re-upload was dropped as unnecessary). ROOT CAUSE: the Supabase project runs in AWS **us-west-2** (DB host's IPv6 confirmed against AWS ip-ranges.json) while Vercel functions defaulted to **iad1** (Virginia), so every request paid 1–3 cross-country Supabase round trips (~300–700ms each — the route's count/ranks/history batch plus the 1000-row sample). FIX: `vercel.json` with `"regions": ["pdx1"]` — Vercel's Portland region, the same AWS region as the DB. Compute moves next to the data; the user→function hop grows ~70ms for East Coast users but each request's multiple DB round trips shrink to in-region latency, a large net win. RESULT (prod, measured post-deploy): API TTFB 0.7–2.2s → **0.29–0.54s**; first-load (API + art sequential) ~2–4s → ~1.3s; art misses ~0.85s (unchanged — that path is dominated by Supabase's storage service + function invoke, not geography) but now sit under the client's 1s `ART_GRACE_MS`, and the 2–3x faster refills keep the challenger queues full, so the mid-game grey pulse should effectively disappear. Verified post-deploy: `x-vercel-id: iad1::pdx1::…`, art edge HIT 0.08s with correct immutable headers, API payload sane, homepage 200. Files: `vercel.json` (new).

- [x] (**My Top 8 share**) Original ask: implement GROWTH_STRATEGY.md's share loop — "a My Top 8 section shareable via text, reddit, url whatever" showing a powerful snapshot of the user's experience; "remember to make it pretty." THE LINK CARRIES THE CARDS, NOT THE PLAYER: `/top8/<code>` packs the eight card_ids' raw uuid bytes as base64url (171 chars, `lib/top8.ts`, strict-validated on decode so junk codes 404 before any DB query). A playerId in the URL was rejected deliberately — `/api/rankings?playerId=` is open by design AND the anonymous uuid is literally `transfer_player_data`'s ownership proof, so a playerId-bearing link would hand every recipient the sharer's identity (and for anon players, the power to adopt their whole history into a fresh account). Packing the cards also makes each share an immutable snapshot: the page never drifts as the sharer keeps ranking. PAGE (`src/app/top8/[code]/page.tsx`): server-rendered poster — "Top 8" with the red 8, 4×2 grid (2-col on phones) of proxied art with white rank badges hung off the corners (#1 red), one "Rank your own cards" CTA into /compare; no session needed, so every scraper/recipient sees it. OG IMAGE (`opengraph-image.tsx`, next/og file convention — auto-wires og:image + twitter:image; page metadata sets `summary_large_image`): 1200×630 — CardMash racetrack pill + "MY TOP 8" + cardmash.io rail beside the 4×2 art grid with the same badge language. Satori can't decode WebP, so it inlines the ORIGINAL pkmncards JPEGs (`image_url`, browser UA — same bot-challenge dodge as the backfill) as data URIs; fetch failures degrade to a neutral slot, never a failed image; scraper-only traffic makes the upstream load negligible. Fonts are 2.8KB/14KB subsetted TTFs (wordmark + title glyphs only) committed beside the route, read via `readFile(join(process.cwd(),…))` — the `fetch(new URL(…, import.meta.url))` docs pattern 500'd in the Node runtime (undici won't fetch file:), and the cwd-join literal is what Next's file tracing needs to deploy the fonts. BUTTON (`ShareTop8Button`, Rankings toolbar, mine scope only — a friend's/community's list isn't yours to share): fetches the TRUE unfiltered top 8 at tap time (whatever filters/search the list shows), then `navigator.share` (native sheet → text/reddit/anywhere; canceled sheet swallowed) with clipboard + "Link copied" flash fallback; <8 ranked flashes "Rank 8+ cards to share". VERIFIED: codec round-trip + 4 malformed-input rejections via tsx; against the live dev server — page 200 with all 8 cards in rank order, og:image/twitter meta correct, junk + truncated codes 404, OG renders (inspected the PNG: wordmark/rail/8 arts/badges all correct); driven Chrome 9/9 — desktop + phone screenshots (clean at both widths), fresh-player toast, stubbed navigator.share receives the exact URL + title, clipboard fallback copies the URL and following it renders the same 8 cards. tsc + eslint clean. Files: `src/lib/top8.ts`, `src/app/top8/top8Cards.ts`, `src/app/top8/[code]/page.tsx`, `src/app/top8/[code]/opengraph-image.tsx`, `src/app/top8/*.ttf` (2), `src/components/ShareTop8Button.tsx`, `src/app/rankings/page.tsx`.

- [x] (**card-art proxy + CDN cache**) Original ask: implement the backlog idea "proxy + cache the card-art bucket (immutable WebP, cache-everything) so art stops burning Supabase egress." WHY: every art view downloaded straight from Supabase Storage, and free-tier egress (~5GB/month ≈ ~70k loads at ~70–90KB) was the standing ceiling flagged in the self-hosted-art task; DNS check showed cardmash.io points straight at Vercel (no Cloudflare), so the cache layer is Vercel's edge CDN, driven by response headers we control. FIX: new `/art/[file]` route handler streams `card-art/{card_id}.webp` from the public bucket back with `Cache-Control: public, max-age=31536000, s-maxage=31536000, immutable` — `s-maxage` is what makes Vercel's edge cache function responses (stripped before the client sees it), `max-age`+`immutable` is what browsers keep, and year-long is safe because the backfill never overwrites an object at an existing name (changed art would get a new card_id). The segment is validated against a strict `{uuid}.webp` regex BEFORE building the upstream URL — Next decodes `%2F` in params and fetch normalizes `../`, so a loose check would let `/art/..%2F..%2F<other-bucket>%2F...` escape the bucket; misses return 404 `no-store` so the CDN never memorizes an anomaly, and the upstream fetch is `cache: "no-store"` to keep image bytes out of Next's data cache (the edge cache IS the cache). `withStorageArt` rewrites `art_url` → `/art/{basename}` in the one shared fold (DB keeps the full Supabase URL as source of truth); the comparison route's hand-rolled `art_url ?? image_url` fold was collapsed into the helper (it was `withStorageArt` written out longhand). NO key bump: pre-change sessionStorage saves hold absolute Supabase URLs, which still resolve (bucket stays public) — old saves degrade to direct fetches once, new payloads all proxy. HONEST LIMIT: Vercel's edge cache is per-region and flushed on every deploy, so Supabase still sees ~one fetch per card per region per deploy — but browsers hold their immutable copies ACROSS deploys, so egress drops from per-view to near-zero for return traffic. VERIFIED against the live dev server: proxy bytes `cmp`-identical to the Supabase object with correct headers; traversal attempt / junk name / unknown uuid all 404; `/api/comparison/next` emits `/art/...` paths; driven Chrome renders Play and Rankings art painted via the proxy (11 art requests, 0 non-200). Post-deploy check for prod: second request for any art URL should return `x-vercel-cache: HIT`. Files: `src/app/art/[file]/route.ts` (new), `src/lib/cardArt.ts`, `src/app/api/comparison/next/route.ts`.

- [x] (**true ranks under Rankings filters**) Original ask: "when i filter on my rankings page, the numbers next to each card increase sequentially instead of being the true ranking of that card. Change them to the true ranking." Extends the search fix's lookup-not-re-ranking semantics (DONE: rankings-search-true-rank) to the series/price filters: the number beside a card is now ALWAYS its true position in the full list, so filtered lists show gapped ranks (3, 10, 15…). PERSONAL: `buildRankMap` now runs whenever ANYTHING narrows the list (filters or search, not just `namePattern`) and builds positions from the player's UNFILTERED ranked list — the `cards!inner` join and the re-applied series/price clauses were deleted, leaving a bare paged `card_ranks` scan; the paged `from + index + 1` remains both the no-filter path and the map-failure degrade. UNIVERSAL: the incrementing filtered `rank` counter became `i + j + 1`, the card's index in the full Bayesian-ordered leaderboard. TIE BUG caught by verification, not review: cross-checking filtered ranks against unfiltered positions flagged Centiskorch VMAX off by one — four cards tie at r=1362.31…, and Postgres returns equal-`r` rows in ARBITRARY per-query order, so the (join-shaped) list query and the (bare) map query numbered the tie group differently; fixed with a `.order("card_id")` tiebreak on BOTH queries so ties resolve identically everywhere. VERIFIED against the live dev server with the heaviest real player (2,710 ranks — exercises the map's >1000-row paging): after the tiebreak, 46/46 cross-checkable cards match across price-filtered, series-filtered, and search+filter responses (1 mismatch before); universal filtered scope 34/34; filtered page-1 ranks (783/791/798/811) match SQL `row_number() over (order by r desc, card_id)` exactly. No client change — the page renders `card.rank` as sent. Files: `src/app/api/rankings/route.ts`.

- [x] (**eBay affiliate live + per-card revenue tracking**) Original ask: "I was approved for eBay affiliate… here: 5339165455. Also implement the per card revenue tracking." The plumbing already existed from the eBay-button task — `ebayUrl` carried the full EPN parameter set (mkcid=1 affiliate channel, mkrid=711-53200-19255-0 US rotation id, siteid=0, toolid=10001, mkevt=1) behind a null `EBAY_CAMPAIGN_ID` placeholder, so activation was pasting the approved campaign id in. TRACKING: every eBay link now also carries the card's `card_id` as EPN's `customid` (free text ≤256 chars, echoed verbatim in EPN transaction reports) so purchases attribute to the exact card that drove them; reports show the UUID, which joins back to a name via the `cards` table. THREADING: `ebayUrl` gained an optional `cardId` param; `CardBack` gained a `buyCardId` prop passed by both flips (Play `ComparisonArea`, Rankings page). Unlike its optional siblings (`buyProductId`/`buyPack`, nullable because pre-change sessionStorage saves lack them), `buyCardId` is REQUIRED — `card_id` predates every saved Card shape, so no save can lack it and no key bump was needed; required also makes TypeScript enforce the pass-through at both call sites. Compliance unchanged (rel="sponsored" + the FTC disclosure line already on the card back). VERIFIED in driven Chrome against the live dev server: flipped a Play card, parsed the rendered href — campid=5339165455, customid=<that card's uuid>, mkcid=1, search query intact; tsc clean (the one lint error is the pre-existing Clefairy set-state-in-effect on main). Files: `src/lib/cardInfo.ts`, `src/components/CardBack.tsx`, `src/app/compare/ComparisonArea.tsx`, `src/app/rankings/page.tsx`.

- [x] (**grey cards before art fills**) Original ask: "fix how sometimes cards load in grey before the art fills." DIAGNOSIS: every mount path (mountPair, swapLoserForFresh) started the below→center slide the moment its slot was free, without consulting decode state — any card whose art wasn't downloaded+decoded within the ~350ms slide arrived at center as the pulsing grey skeleton and filled in there. The overlap fast paths were never affected (they gate on `imageReady`); the leaks were the cold-fetch and queued-but-undecoded paths. FIX: `warmImage` now records a per-URL completion promise (`imageWarmWaits`, resolves on success AND failure so a 404 can't hang a wait); new `artSettled(urls)` kicks off the warms and resolves when all decode or after `ART_GRACE_MS` (1s) — mountPair and swapLoserForFresh await it before starting the slide-in, so cards arrive painted; past the cap the skeleton remains the honest degrade (a dead network must not read as a frozen board). Fetched challengers are warmed at fetch-resolve, not at mount. A `mountSeqRef` counter abandons stale mountPair continuations (the grace widened the window where a filter change could interleave, recentering dropped cards and flipping `ready` early). VERIFIED with a rAF-polling detector in driven Chrome (logs any card centered while its skeleton is still visible, with duration) under CDP throttling: at 8Mbps, 9 grey stretches (100–1225ms) on old code → 1 (325ms) on new; at a saturated 1.5Mbps both builds show grey (grace cap by design — 4+ warmed images share the pipe and blow past 1s); unthrottled swap timings unchanged (33–88ms fast paths). Files: `src/app/compare/ComparisonScreen.tsx`.

- [x] (**instant Shuffle**) Original ask: "is it possible to speed up the card loads after I click shuffle?" DIAGNOSIS: handleShuffle was fully serial — slide-out (350ms), THEN a cold /api/comparison/next fetch (~500–650ms), THEN mountPair with cold art — ~1.2–1.5s total, while ordinary picks were near-instant off the preload queues. FIX: shuffle now draws its replacement pair from the live preload store and overlap-swaps in one motion when the art is decoded, exactly like a preloaded pick: Keep Winner off pops a whole queued pair (`popFreshPair` — precisely what a shuffle wants); Keep Winner on takes one challenger from EACH side's queue/donated lists (`popChallenger` twice, the second excluding the first's card_id — the two sides fetch independently and can queue the same card), both residents' queues deleted since both leave the board. The departing dials get from===to tweens (no rating change; RatingDial's effect early-returns on equal values). pickedId is cleared (no pick happened — a stale winner flash must not remount and replay on the exit overlay). With nothing queued: the pair still slides out immediately, but the fetch now starts AT CLICK TIME and runs during the slide (`fetchNextPair` split out of loadNextPair; loadNextPair accepts the pending promise) instead of after it. MEASURED in driven Chrome: settled-queue shuffles 32–115ms both modes (was ~1.2–1.5s); rapid shuffles that outrun the refill ~380–430ms; undo snapshot still dropped, picks after shuffle still POST 200. Files: `src/app/compare/ComparisonScreen.tsx`.

- [x] (**full-art promos wrongly excluded — Galarian Moltres SWSH284**) Original ask: "Galarian Moltres SWSH284 is a full-art promo, so it should be eligible." DIAGNOSIS: the pool keeps a bare-`Promo` card only when its name carries a featured mechanic (`hasFeaturedMechanic`), on the assumption — stated in the old comparisonPool comment — that "full-art cards always get their own distinct rarity, so this never drops a full art." True for MAIN-SET cards (a full art there is Ultra/Secret/etc.), but FALSE for modern Black Star Promos: full-art and plain framed promos BOTH carry bare `Promo`. Proof: the two Galarian Moltres promos — SWSH125 (framed, Evolving Skies stamp, correctly out) and SWSH284 (full art, wrongly out) — share name AND rarity; the only discriminator is the collector number or the image. Checked pokemontcg.io: it also just says `Promo`/`Basic`, no full-art flag — so NO data column and NO external typed source can tell them apart. User chose the COMPREHENSIVE curated include-list (over a seed-and-grow list, an image detector, or keeping all modern promos which would re-admit hundreds of framed staples). METHOD: pulled all 923 unique bare-`Promo` ineligible cards, downloaded every image, built `sharp` contact-sheet montages, and visually classified full-bleed vs framed art — modern sets reviewed card-by-card, pre-SWSH sets filtered by an automated lower-band colorfulness score (top-24 candidates of all 545 were every one framed → older sets contribute nothing; the SWSH-style full-art basic promo is a 2020+ phenomenon). Caught two thumbnail misreads at full res: the Special Delivery series (Pikachu SWSH074 / Charizard SWSH075 / Bidoof SWSH177) is FRAMED, not full art (stays out); and of the two Marnie supporters SWSH121 is full art while SWSH120 is framed. User approved all three judgment categories: full-art Pokémon, full-art Supporters (Iono/Marnie/Spark/Blanche/Candela/Boss's Orders/Klara — Supporters are already a kept category), and the V-UNION quarter-cards; plus the Ancient Mew novelty. FIX: `FULL_ART_PROMO_NUMBERS` in comparisonPool.ts — a curated include-list keyed by `pack` + `collector_number` (which covers BOTH set-variant duplicate rows per card for free, unlike a card_id key), 54 cards across SWSH/SVP/MEP; Ancient Mew keyed by name (its collector_number is a junk "o Number"). The `Promo` rule became `hasFeaturedMechanic(name) || isFullArtPromo(row)`; `EligibilityRow` gained optional `pack`/`collector_number` and stamp-eligibility.ts selects them. DRY-RUN before syncing (a throwaway diff script): exactly 107 rows GAIN eligibility (54 unique × 2 dupes, −1 for single-row Ancient Mew), 0 LOSE — pure additions, so NO saved-pair key bump needed (additions can't stale a held board). Applied via MCP: staged + synced 6595 → 6702 eligible (verified all 54/107 flipped, 0 framed lookalikes leaked, SWSH284 both rows eligible), then backfilled self-hosted `card-art` storage for all 107 rows (temp anon INSERT policy → `backfill-card-art.ts` uploaded 104 → stamped `art_url` → dropped the policy; final check: 107/107 with storage art, 0 fallbacks). MAINTENANCE: DEVELOPMENT.md notes to review new full-art promos into the list on import. Files: `src/lib/comparisonPool.ts`, `scripts/stamp-eligibility.ts`, `DEVELOPMENT.md`.

- [x] (**Clefairy flees the instant a serpent enters**) Original ask: "Clefairy reacts too slowly when the serpents enter; she should start running as soon as they enter." Iterated with the user on timing (150ms → 500ms notice beats), but the real bug was that the flee was scheduled off `distance ÷ crossSpeed` — the AVERAGE crossing speed — while the serpent glides on a CSS `ease-in-out` transition that starts far slower, so the timer fired while the nose was still off screen. FIX: `watchForNose` polls the serpent positioner's real animating transform (`DOMMatrixReadOnly`, the file's own "real motion can't lie" pattern) and triggers the flee a fixed `SERPENT_NOTICE_MS` (500ms, user-tuned) after the leading edge actually crosses the screen edge — correct for any easing curve or screen width. Also threaded a `lookOverrideMs` through `walkTo` so the panic flee skips the usual 300-650ms look-around beat. NOTE: an early "it's still too fast" report was a STALE deploy (prod served the prior commit until its Vercel deploy finished, and the local dev server served JS from several commits back — the Turbopack `.next` cache affects JS too, not just CSS); measured the true behavior in headless Chrome by sampling `getBoundingClientRect` before trusting the bug report. Files: `src/app/compare/Clefairy.tsx`.

- [x] (**Prism Star Supporter pool leak — Lysandre**) Original ask: "Lysandre 110/131 (a modern 2019 trainer) popped up — this shouldn't happen." DIAGNOSIS: the card is "Lysandre Prism Star" (Forbidden Light, 2018), a SUPPORTER, and it was eligible BY DESIGN — the pool keeps all Supporters (only Item/Stadium/Tool trainers + energies are excluded), and its `Rare Prism Star` rarity is judged by no rarity rule, so `isEligible` fell through to `return true`. Not a broken rule but a policy gap, so it needed a user decision. Surveyed all 27 `Rare Prism Star` cards: 16 Pokémon (kept, correctly — Arceus, Ditto, Giratina, …), 2 energies + 5 Stadiums already excluded, and exactly 4 Supporter trainers leaking: Cyrus, Lance, Lusamine, Lysandre. User chose the SURGICAL fix (drop these 4, keep the beloved full-art Supporters like Cynthia/Marnie) over excluding all Supporters. FIX: a closed `EXCLUDED_PRISM_STAR_TRAINERS` set (Prism Star is a discontinued Sun & Moon-era mechanic, so it can't grow) matched on the FULL lowercased name — NOT `normalizeName`, because that strips "Prism Star" and would also drop the full-art "Cyrus"/"Lysandre" Supporter cards we deliberately keep (the exact collision that makes the bare-name list wrong here). Re-stamped the 4 rows to ineligible via MCP (verified: exactly 4 flipped, 16 Pokémon Prism Stars still eligible, 0 trainer leaks remaining); bumped the saved-pair key v5 → v6 so any held board carrying one of the 4 flushes on reload. NOTE: this is the THIRD name-join-vs-generated-list leak (after Prism Star Stadiums and Hero's Cape) — the standing advice is to switch the exclusion source to typed card data (tcgcsv types keyed by `tcgplayer_product_id`), but an explicit 4-name set is defensible HERE because the mechanic is closed; a fourth leak of a still-printed category should trigger the typed-data refactor. Files: `src/lib/comparisonPool.ts`, `src/app/compare/ComparisonScreen.tsx`.

- [x] (**nav avatar squished on iOS**) Original ask: "on iOS the avatar is smushed width-wise; if you shrink the PokéMash logo a little and move it left, it shouldn't be smooshed." DIAGNOSIS: the 52×52 avatar `<img>` (`AccountControl`) had `w-[52px] h-[52px]`, but on a narrow phone the nav row's contents (100px logo + Play/Rankings + About + avatar pill) overflow the viewport, so flexbox shrinks the account pill; once it drops below 52px, Tailwind Preflight's `img { max-width: 100% }` caps the sprite's WIDTH while the explicit `h-[52px]` pins its height — hence squished width, not height (iOS Safari hits this harder). FIX (both levers): shrink the logo per the user's instinct — logo `w-[100px]` → `w-[80px]` on all viewports and nudge it left with `-ml-1` — AND pin the avatar so it can never compress again: `shrink-0` on the pill button + `max-w-none shrink-0` on the img. Pinning is the load-bearing lever: verified in Chrome at 375px that the old classes render the avatar 32×52 (squished) while the shipped classes render a true 52×52, no page overflow. (Note: on a narrow phone the row is genuinely full, so flexbox further shrinks the LOGO image — correctly proportioned, just small — which is fine now that the avatar no longer competes for that space.) Files: `src/components/NavBar.tsx`, `src/components/AccountControl.tsx`.

- [x] (**curly-apostrophe pool leak — Hero's Cape**) Original ask: "Hero's Cape 152/162 (2024 Pokémon Tool) showed up; Tools should never appear — what happened?" DIAGNOSIS: the exclusion list HAS "hero s cape" — but the catalog writes names with the typographic apostrophe ("Hero’s Cape", U+2019) while the list was generated from the API's ASCII "Hero's Cape". `normalizeName`'s non-ASCII drop DELETED the curly form ("heros cape") while the ASCII form's punctuation-to-space made "hero s cape" — same word, two different normalizations, join never matches; rarity `ACE SPEC Rare` is judged by no other rule, so it stayed eligible. Same root cause as yesterday's Prism Star leak: a name join whose two sides spell the same thing differently, with the normalizer treating the variants inconsistently. FIX: map `[’‘]` → ASCII `'` BEFORE the non-ASCII drop, so both forms funnel into the same punctuation-to-space path. Re-stamp: 6,606 → 6,599, and the 7 flipped cards were verified BY NAME before syncing — all genuine Items/Tools/Stadiums (Hero's Cape, Hero's Medal, Lana's Fishing Rod, Lillie's Poké Doll, N's PP Up, Team Rocket's Transceiver, The Rocket's Training Gym); zero Supporters/Pokémon touched (the ~250 other curly-apostrophe cards are deliberately-kept Supporters, Gym-era Pokémon, Team Rocket's ex Pokémon, etc.). Post-sync leak query: 0. NOTE for the future (also a LEARNING entry): two leak classes in two days came from the name-join-vs-generated-list pattern — if a third appears, consider swapping the exclusion source to typed card data (tcgcsv card types keyed by the `tcgplayer_product_id` already stamped on 98.6% of the catalog) instead of patching the normalizer again. Files: `src/lib/comparisonPool.ts`. FOLLOW-UP (still seen after the DB fix): the card kept appearing on reload even though the API can no longer serve it — the `readSavedComparison` restore path (sessionStorage `pokemash:comparison`) runs on mount BEFORE any `/api/comparison/next` call and survives reloads, so a pair saved while Hero's Cape was still eligible replayed the stale board. The API filter (`eq eligible true`) was never the leak here; the client cache was. Fix: bump the saved-pair key v4 → v5 (the codebase's existing flush mechanism, previously only used for Card-shape changes) so pre-fix pairs are discarded once; documented that an eligibility re-stamp is now also a reason to bump. Files: `src/app/compare/ComparisonScreen.tsx`.

- [x] (**2× nav avatar + two pool-eligibility leaks**) Original ask: signed-in avatar twice as big; "Wondrous Labyrinth 158/181 (modern trainer) and Holon Energy 86/101 shouldn't appear — never show energy." AVATAR: pill sprite 26px → 52px (`AccountControl`), nav bar grows 68→84px to fit (pill py-2 rides on the image box); pixel art stays crisp via image-rendering:pixelated. POOL LEAK 1 (Holon Energy FF/GL/WP, 6 rows): the trio suffixes element letters AFTER "Energy", dodging `isEnergyCard`'s last-word anchor, and their vintage `Rare` rarity sailed through the keep-vintage-rares path — added a `/^Holon Energy\b/` exception; a word-boundary DB sweep (`name ~* '\menergy\M'` over eligible rows) proved they were the ONLY leaked energies, so the last-word rule stays the general case. POOL LEAK 2 (5 Prism Star stadiums: Wondrous Labyrinth, Black Market, Heat Factory, Life Forest, Thunder Mountain): the excluded-trainer list was generated from the API where the tag is "◇" (dropped as non-ASCII → "wondrous labyrinth"), but our catalog spells out "… Prism Star", so the normalized-name join never matched, and rarity `Rare Prism Star` is judged by no other rule — `normalizeName` now strips the spelled-out tag (lesson: a normalization function shared with a generated list is part of the data contract; Pokémon/Supporter Prism Stars — Ditto, Cyrus, etc. — remain correctly eligible). Re-ran stamp-eligibility + sync UPDATEs via MCP: 6,617 → 6,606 eligible, exactly the 11 leaked cards, and a post-sync leak query returned 0; no art backfill needed (removals only). Verified the 52px pill with a throwaway password-session user (Snorlax visible, box measured 52×52; user deleted after — and noticed the FIRST REAL Google profile exists in prod, so OAuth is confirmed working in the wild). Files: `src/components/AccountControl.tsx`, `src/lib/comparisonPool.ts`.

- [x] (**friends + Gen 1 avatars**) Original ask: once signed in, add friends and see their rankings; pick a personal avatar from 20 popular Gen 1 Pokémon. (Google provider still awaiting Liam's dashboard config — everything verified with password-session test users per the testing-signed-in-flows memory.) SCHEMA (`supabase/migrations/20260707_profiles_and_friends.sql`, applied to the remote): `profiles` (user_id → auth.users, display_name defaulted from the email local part, avatar slug, unique 8-char `friend_code`) + `friendships` (one canonical row per pair, `check (user_a < user_b)` — no mirrored duplicate to keep in sync; FKs to profiles so deletes cascade). PRIVACY-FIRST ADD MODEL: friends are added by exchanging codes, never by searching emails — handing someone your code is both the request and the consent, so the friendship is mutual on entry with no request/accept queue to manage (deliberate minimalism + consent-in-one-step). No public select on profiles: code lookup happens inside `add_friend(code)` SECURITY DEFINER (raises on unknown code / own code; re-add is a no-op), so the table can't be enumerated; `ensure_profile()` (also definer, idempotent, called lazily by the client on sign-in) mints the row + code (alphabet drops 0/O/1/I/L, collision-loop). RLS: read own + friends' profiles only; update own; friendships select/delete only for your own rows; anon fully revoked (verified 42501 on both RPCs and both tables); column-level `grant update (display_name, avatar)` keeps friend_code immutable from the client. AVATARS: 20 sprites (96×96 pixel art, ~1KB each, PokeAPI GitHub) in `public/avatars/`, catalog in `src/lib/avatars.ts` (`avatarSrc` returns null for unknown slugs → degrade to the person glyph, never a broken img); rendered as plain `<img>` with `image-rendering:pixelated` (next/image buys nothing at 1KB). `AvatarPicker` modal: 5×4 grid, pick = save + close (no confirm button), current choice ring-red, HowItWorks overlay conventions. `AccountControl`: pill shows the chosen sprite instead of the person glyph (optimistic update, persists via profiles read on reload); dropdown grew Friends / Choose avatar entries. UI: no new top-level nav item — Friends lives behind the account control (minimalism). `/friends` page: your code + Copy, an add-by-code form (friendly error mapping for the RPC raise messages), friend rows (avatar + display_name → their rankings; hover-reveal × to remove, covering both row orders in the delete filter). FRIEND RANKINGS: reuses the rankings page with `?player=<uuid>` (linked from friend rows) — same fetch path with their id, scope toggle replaced by "«name»'s rankings" + ← Friends, meter rephrased ("«name» has compared…"); the name resolves through the friends RLS policy so a random uuid pasted in the URL shows "A friend's rankings" (verified post-removal). `useSearchParams` forced the screen behind a `Suspense` boundary (Next prerender requirement). HONESTY NOTE: the rankings DATA itself still flows through the open `/api/rankings?playerId=` endpoint (pre-existing open-RLS design for anonymous play) — the friends feature enforces name/avatar/code privacy, and rankings-by-uuid remains guessing-proof but not RLS-proof; tightening that means authing the rankings route someday. LEGAL: privacy page gained the profile fields and a Friends section (code-only discoverability, what friends can see, removal is bidirectional) in the same PR per its header rule. VERIFIED end-to-end in installed Chrome, two throwaway users: 21/21 — dropdown items, picker grid, Gengar pick reflected in pill + persisted across reload, own code format, own-code + bogus-code errors, B adds A by code, A's name/avatar in B's list, friend rankings header/cards/meter, scope toggle hidden, removal empties the list AND kills name resolution; anon locked out everywhere; test users + all their rows deleted after. Files: `supabase/migrations/20260707_profiles_and_friends.sql`, `public/avatars/*` (20), `src/lib/avatars.ts`, `src/lib/profile.ts`, `src/components/AvatarPicker.tsx`, `src/components/AccountControl.tsx`, `src/app/friends/page.tsx`, `src/app/rankings/page.tsx`, `src/app/privacy/page.tsx`.

- [x] (**Google sign-in + anonymous-history transfer**) Original ask: sign-in for continuity/personal rankings via Google OAuth on Supabase's built-in auth; local comparisons auto-transfer to a fresh account behind a "Would you like to transfer your local data to your account?" modal; the user decides when to sign in; Sign Up + Login buttons (design delegated); signed in, a person icon in the upper-right opens a dropdown with the email and a sign-out button; handle private data appropriately. KEY INSIGHT: "local" progress was never local — `card_ranks`/`comparisons` already live in Supabase keyed by the localStorage anonymous UUID (`pokemash_player_id`), so transfer = re-keying rows, not uploading anything. IDENTITY: `getPlayerId()` (lib/playerId.ts) became async — signed-in Supabase user id ?? anonymous UUID — so every existing read/write path works for both identities with no route changes (routes already take playerId explicitly); call sites adapted (loadNextPair/preloadNext/undo await it; `handlePick` chains it into the fire-and-forget POST so the pick handler stays synchronous for the animation work; `swapLoserForFresh` resolves it internally, dropping its param). AUTH: pure Supabase Auth PKCE — `signInWithOAuth({provider:"google"})` from the client, new `/auth/callback` route handler exchanges the code via the existing `utils/supabase/server` client (failure = land home signed out, never an error page); no new env vars, no middleware (client refreshes its own tokens; server routes don't require auth). NEEDS ONE-TIME DASHBOARD CONFIG (documented in DEVELOPMENT.md): Google Cloud OAuth client + enable the provider + allow `{origin}/auth/callback` redirect URLs — until then the Google button bounces with "provider is not enabled". UI (minimalism call, flagged): ONE "Sign in" pill replaces the planned Sign Up + Login pair — with OAuth they're the same action, two buttons would dead-end in the identical Google screen; the modal's microcopy carries sign-up ("New here? The same button creates your account"), plus Google-branded button and a data-collection line linking /privacy. Signed in, the pill becomes a person icon (stroke SVG, pill hover red) with a dropdown (email + Sign out; invisible-backdrop click-outside). Sign-out reloads the page — every visible surface belongs to the identity, a reload restarts cleanly as anonymous. TRANSFER: `TransferPrompt` (layout-mounted) fires once per account per browser (localStorage `pokemash_transfer_resolved:<userId>`): on sign-in, if the anon id exists with comparisons AND the account has none (fresh — two Glicko histories for the same card can't merge), a modal offers Transfer / Start fresh with the real count. Transfer calls `transfer_player_data(anon_player)` — SECURITY DEFINER (RLS has no UPDATE on comparisons; app holds only the publishable key), destination forced to `auth.uid()` (caller can't pick it), EXECUTE revoked from anon (verified: 42501), fresh-account rule re-checked server-side; possession of the unguessable anon UUID is the ownership proof. Then the anon id is cleared (else it'd resurface an empty history on sign-out) and the page reloads. Decline records the answer and leaves the anon history reachable when signed out. LEGAL: /privacy page (plain-language: what's stored — email + comparison history, nothing else; no ads/profiling/selling; auth-only cookies; affiliate links send nothing; delete-on-request via GitHub issue), linked from the sign-in modal; data minimization otherwise (only basic profile+email scopes). VERIFIED end-to-end in installed Chrome: signed-out smoke (pill/modal/privacy/pick-recording regression) all pass; full signed-in flow driven with a throwaway email+password session written through the same @supabase/ssr cookie storage the app reads (Google needs the dashboard config, but everything after the callback is identical) — transfer modal with correct count, RPC re-keyed 2 comparisons + 3 ranks to the account (0 left on the anon id), anon id cleared, rankings owned by the account, dropdown email, sign-out back to the pill; test user then deleted. Files: `supabase/migrations/20260707_player_data_transfer.sql`, `src/lib/playerId.ts`, `src/app/auth/callback/route.ts`, `src/components/AccountControl.tsx`, `src/components/TransferPrompt.tsx`, `src/components/NavBar.tsx`, `src/app/layout.tsx`, `src/app/privacy/page.tsx`, `src/app/compare/ComparisonScreen.tsx`, `src/app/rankings/page.tsx`, `DEVELOPMENT.md`.

- [x] (**refresh market prices from TCGplayer**) Original ask: shown prices are off — "Base Set Charizard $600 vs $1,500 NM on TCGplayer, Expedition Mew $200 vs ~$5,000". Diagnosis first: our price columns were a one-time import gone stale, but the quoted site numbers were partly different PRINTINGS — TCGplayer's own market price for unlimited Base Set Charizard is $728 (the $1,500+ figures are Shadowless at $2,146 / 1st Edition at $10,000), and Mew's "$5,000" is a reverse-holo LISTING ask (mid $4,994.98) whose market is $216–412; asking prices ≠ market. FIX: `scripts/refresh-prices.ts` re-reads current prices from tcgcsv's daily mirror of TCGplayer's price feed, joined on `cards.tcgplayer_product_id` (no name matching — the id backfill is the join), staged into `price_stage` (same anon-writable pattern; migration `20260707_price_refresh.sql`) and synced by a printed UPDATE. TCGplayer prices are per PRINTING (`subTypeName`), so `printingRank` prices each card's base printing: unlimited Normal/Holofoil first (Holofoil preferred when the rarity contains "Holo" — this is exactly what fixed Mew 19/165, whose old $216.50 was the Reverse Holofoil market riding a catch-all import), then Reverse Holofoil, then 1st Edition, taking the best-ranked row that has a non-null market. `market_price` gets TCGplayer market (recent sales); `lowest_price`/`highest_price` get the same printing's listing bounds (columns keep their semantics). RESULT: 22,457/22,581 id-carrying cards priced (rest keep old values — stale beats absent for a column the price FILTER ranges over); 22,756 cards now hold a positive market price; median absolute change $0.04 (bulk stable, chase cards moved: Charizard 644→728, Mew 216→412, Moonbreon 2,210→2,321). No app code changed — the columns feed the existing filter and price rows. FOLLOW-UP (near-mint verification): user pushed back — the TCGplayer PAGE shows Near Mint N/A for Expedition Mew while the feed said $411.66, and "Dark Vaporeon $27" turned out to be the page's Price Points widget set to DAMAGED Holofoil (the NM comparison line on the same page reads $101.12 ≈ our $101.13; diagnosis by rendering the pages in Playwright Chrome and sniffing the page's API calls — tcgplayer.com is a client-rendered shell, curl sees nothing). The feed's market price is printing-level and can be carried entirely by played-copy sales on scarce vintage; the page's headline is NM-specific. FIX (v1, superseded): null market_price when the printing had zero NM sales this quarter — WRONG proxy, it blanked Expedition Feraligatr whose page shows an NM price computed from OLDER sales; lesson: read the displayed state, don't proxy it through a correlated signal. FIX (v2, shipped): for cards ≥ $25 (`NM_CHECK_THRESHOLD`, ~2.5k distinct products) resolve the chosen printing's **Near Mint SKU** (TCGplayer prices per sku = printing × condition × language; sku list from `mp-search-api…/v2/product/{id}/details`, sku market prices batch-POSTed to `mpgateway…/v1/pricepoints/marketprice/skus/search` — both public page APIs, no mpfev needed) and stamp THAT sku's market price — the literal number on the page's "Near Mint Comparison Prices" line — or null when TCGplayer prices no NM sku (2,725 stamped, 162 nulled → em dash; nulled cards drop out of price-filtered pools like never-priced cards). Failure posture: a failed details request keeps the feed price — only a positive answer from TCGplayer may change a number. Cheap cards keep the unverified feed price (NM sales dominate their markets; label is approximate there). The label on both flips is now **"Near Mint Market Price"** (user request; wraps to two lines on the 238px rankings back — acceptable, nothing clips). Verified: Feraligatr 12/165 — → $331.83 (user's "$153" was the page's REVERSE holo NM line), Dark Vaporeon $101.12 (page-exact), Expedition Mew 19/165 "—". FOLLOW-UP 2 (vintage-only + price the printing in the picture): user caught Neo Discovery Umbreon showing the UNLIMITED price ($74) under a scan that visibly carries the 1st Edition stamp — checked our stored art for Base Set/Jungle/Gym/Team Rocket/Neo by eye: pkmncards scans for every WOTC pack with a 1st Edition print run are 1st Edition copies. So (1) the NM check now runs ONLY for vintage cards (comparisonPool's `isVintage`, pre-Black & White, newly exported — modern cards trade NM constantly so feed ≈ NM there; verify set shrank ~2.5k→~2k), and (2) cards in `FIRST_EDITION_SCAN_PACKS` (BS/JU/FO/RO/G1/G2/N1-N4) are ALWAYS verified (any price) and priced as **1st Edition** — `printingRank` prefers the matching edition, and the NM sku lookup takes any "1st edition" variant, holo-preferred. Base Set special case: TCGplayer files Base 1st Editions (= Shadowless) under the separate "Base Set (Shadowless)" group, so those cards resolve their NM sku on the Shadowless product matched by collector number — BUG on first run: Shadowless zero-pads numbers ("004/102" vs our "4/102"), the simplified key didn't strip zeros, and the whole pack silently KEPT unlimited prices (failure posture made the miss invisible — nothing crashed); fixed with the backfill's digit-run normalization. Net: 2,000 stamped/150 nulled; Base Alakazam $429, Raichu $467.50, Venusaur $72.87, Charizard "—" (raw NM 1st-ed Zards only trade graded), Neo Umbreon/Lugia "—", TR Dark Vaporeon $23.29 (1st ed). Sub-$25 non-WOTC vintage keeps the feed price unverified. MAINTENANCE: DEVELOPMENT.md step 7 — prices drift continuously, re-run any time, also after imports (needs step 6's ids first). Files: `scripts/refresh-prices.ts`, `supabase/migrations/20260707_price_refresh.sql`, `DEVELOPMENT.md`.

- [x] (**market price on the Play flip**) The Play screen's card-info back now shows the same "Market Price" row the Rankings flip has. `/api/comparison/next` sends `market_price` per card (both selects + the response map — it was already on the row for the price filter, just stripped from the payload); the client `Card` field is optional like its siblings so older saves render an em dash, and the sessionStorage key bumped v3→v4 (saved-Card shape rule). `formatPrice` moved from rankings/page.tsx to lib/cardInfo.ts beside orDash/packName — it encodes the market_price 0-sentinel rule ("no sales data" → em dash), which must not diverge between the two flips — and widened to accept `undefined` for the Play side's optional field (0 stays falsy, so the sentinel check was untouched). Verified in installed Chrome: five-row back with `$2.87` on a priced card, em dash on a 0-sentinel card. Files: `src/lib/cardInfo.ts`, `src/app/rankings/page.tsx`, `src/app/api/comparison/next/route.ts`, `src/app/compare/ComparisonArea.tsx`, `src/app/compare/ComparisonScreen.tsx`.

- [x] (**direct TCGplayer Buy links**) Original ask: the Buy button's TCGplayer URL just ran a name search; make it land on the actual card's page. TCGplayer product URLs are `tcgplayer.com/product/{productId}` (the id is mandatory — the slug alone doesn't resolve), so this was a data problem: backfill `cards.tcgplayer_product_id` and build the URL from it. IDS: tcgcsv.com is a free daily mirror of TCGplayer's own catalog API (categories/groups/products; category 3 = Pokémon, 217 groups, ~32k products; note it 401s default HTTP-library user agents — send any custom UA). Card products carry `Number` in extendedData in the same shapes as our `collector_number` ("001/086", vintage "1/102", promo "SWSH001"). MATCHING (`scripts/backfill-tcgplayer.ts`, same staging pattern as stamp-eligibility: fills anon-writable `tcgplayer_stage` over REST, prints sync UPDATEs for a DB admin): two-level join — pack→group, then normalized collector number within the group. Group mapping is SCORED, not trusted: candidates come from normalized name variants (strip "SWSH05:"/"SM - " prefixes; fold &↔and, drop apostrophes) plus abbreviation equality, then each candidate is scored by the fraction of the pack's number keys it contains and must clear 0.5 — because TCGplayer abbreviations collide with ours (our "Battle Styles (BST)" is their "EX Battle Stadium"; our "…Classic—Blastoise (CLB)" is their "Celebrations", which would have produced VALID-LOOKING wrong links since low numbers exist in both). ~40-entry `PACK_GROUP_ALIASES` covers the mechanically un-matchable (TCGplayer merges trainer-kit halves into one group, renames promos — "Wizards Black Star Promos"→"WoTC Promo" — and files our standalone "Shiny Vault" pack under "Hidden Fates: Shiny Vault"). Gallery sub-sets tcgplayer splits into own groups (Trainer Gallery/Shiny Vault/Galarian Gallery/Radiant Collection/Classic Collection) fold into the parent's number index — their prefixed numbers (TG21, GG09, SV55) can't collide. Multi-hit number keys are variant listings of the same card (Staff/Prerelease/Pokemon-Center stamps, merged kit decks): prefer the product naming our card, then our kit half, then the shortest (plain) name — any variant is still the right card for a Buy link. RESULT: 22,581/22,898 matched (98.6%); unmatched = unnumbered energies + tiny oddball promos (Box Topper, Futsal, Victory Medals), and only 148 of the eligible pool; name-sanity check showed all "mismatches" benign (Nidoran ♀ vs "Nidoran F", "{R}" vs "Fire"). Spot-verified famous cards (Base Set Charizard 42382, alt-art Umbreon VMAX 246723) against tcgcsv's canonical URLs — tcgplayer.com pages are client-rendered shells, so curl can't confirm titles; verify via the catalog instead. APP: both API routes send `tcgplayer_product_id` (rankings via the shared `CARD_COLUMNS`); `tcgplayerUrl(name, productId?)` in cardInfo.ts replaces `tcgplayerSearchUrl` — product page when the id exists, name search otherwise (mirrors the art_url??image_url degrade posture; also where the affiliate partner code goes once approved, single build site); CardBack takes optional `buyProductId`; sessionStorage key bumped to `pokemash:comparison:v3` (saved-Card shape rule — a restored held winner never refetches). MAINTENANCE: DEVELOPMENT.md new-set procedure gained step 6 (re-run backfill-tcgplayer; newly unmapped packs go in PACK_GROUP_ALIASES). Verified end-to-end in installed Chrome (Play flip + Rankings flip both link product pages; cross-checked the id against the catalog). Files: `scripts/backfill-tcgplayer.ts`, `supabase/migrations/20260707_tcgplayer_product_id.sql`, `src/lib/cardInfo.ts`, `src/components/CardBack.tsx`, `src/app/api/comparison/next/route.ts`, `src/app/api/rankings/route.ts`, `src/app/compare/ComparisonArea.tsx`, `src/app/compare/ComparisonScreen.tsx`, `src/app/rankings/page.tsx`, `DEVELOPMENT.md`.

- [x] (**self-hosted card art + materialized eligibility**) Root cause of "some card arts don't load" in prod: every `<Image>` routed through Vercel's optimizer, whose Hobby-tier transformation quota (5k/month) the ~7k-card catalog exhausted — uncached art 402'd (`OPTIMIZED_IMAGE_REQUEST_PAYMENT_REQUIRED`); pkmncards sources were all fine. FIX (art): `scripts/backfill-card-art.ts` downloads each servable card's JPEG (browser UA; pkmncards bot-challenges plain agents), resizes to 650px-wide WebP (2× the Play screen's 325px for retina; source is 734px so barely downscaled, but JPEG→WebP q80 cuts ~200KB→~70-90KB) via sharp, and uploads to the public `card-art` bucket as `{card_id}.webp` — idempotent (HEAD-check skip), 5-way concurrency, 6,742 uploaded (eligible ∪ ranked). Uploads used a TEMPORARY anon INSERT policy on storage.objects (dropped right after; repo only holds the publishable key). `cards.art_url` is stamped by a SQL join against storage.objects, so it exactly matches what uploaded. Both API routes fold it via `withStorageArt` (lib/cardArt.ts) into the `image_url` the client already reads — art_url null falls back to the pkmncards source, so a stale backfill degrades, never breaks. `images.unoptimized: true` turns the optimizer off (art is pre-sized; optimizing Supabase URLs would just re-blow the quota). FIX (eligibility, follow-up refactor): the pool rules ran in JS per request in three places; now `cards.eligible` is a stamped column. `scripts/stamp-eligibility.ts` evaluates `isPoolEligible` (new combined rarity+name/era rule in comparisonPool.ts) over the catalog and fills `eligible_stage` over REST (anon-writable staging table — harmless, only read at sync time; PostgREST DELETE needs a filter, `card_id=not.is.null` matches all), then prints two UPDATEs for a DB admin (SQL editor or MCP). Stamped 6,617 of 22,898 — EXACT parity with the old JS pool build, verified against prod before switching. Comparison route: count/sample filter `eligible = true` (no rarity not-in, no per-row isEligible); rankings meter: the module-scope paged-pool-promise cache deleted for a head count query sharing seriesOrFilter/price bounds with the numerator. Era/series JS trims and minElo stay (free-text dates, per-player ratings). The never-applied SQL mirror (20260630_comparison_pool.sql) was superseded and removed; applied migrations recorded in supabase/migrations/20260707_*.sql. MAINTENANCE after catalog imports or rule changes: stamp-eligibility → sync SQL → backfill-card-art (art_url stamp join in the migration file header). Known costs: Supabase free-tier egress (~5GB/mo ≈ ~70k image loads) is the new ceiling; ~500MB of the 1GB storage used. ALSO this session: Supabase MCP OAuth ("unrecognized client id" = stale dynamic client registration in the macOS Keychain `Claude Code-credentials` blob — delete the `mcpOAuth` supabase entry, re-authenticate; the localhost callback URL can be relayed from a collaborator's browser if they own the Supabase org, but paste it fast, the flow times out). Files: `scripts/backfill-card-art.ts`, `scripts/stamp-eligibility.ts`, `src/lib/cardArt.ts`, `src/lib/comparisonPool.ts`, `src/app/api/comparison/next/route.ts`, `src/app/api/rankings/route.ts`, `next.config.ts`, `supabase/migrations/20260707_card_art_storage.sql`, `supabase/migrations/20260707_card_eligibility.sql`, package.json (sharp, tsx devDeps).

- [x] (**compared-cards percentage + retro logo restore**) Two user asks. (1) PERCENTAGE: the rankings meter now reads "You've compared 1,180 cards (18%)". The denominator is the TRUE eligible pool, not a raw DB count — the eligibility rules (free-text `release_date` era checks, name-based energy/Item/Stadium matching, mechanic-vs-vintage `Rare`/`Promo` judgment) only run in JS, which is exactly why the old meter showed no denominator at all. `/api/rankings` pages the rarity-prefiltered catalog through the same `isEligible` the Play route uses (rules + `matchesSeries` + `LEGENDARY_COLLECTION` constants extracted to shared `src/lib/comparisonPool.ts`, de-duped by `card_id` like the comparison pool), caches the build as a module-scope PROMISE (concurrent first requests share one build; a failed build nulls the cache so the next request retries), runs it in `Promise.all` beside the ranks query, and re-applies the active series/price filters so numerator and denominator measure the same population. A pool-build failure degrades to the plain tally (`poolTotal` omitted → client shows no %) — progress decoration never breaks the list. (2) LOGO: the animated typed `LogoWordmark` never matched the app's retro-Nintendo feel; the original bubbly pokeball PNG (`/pokemash_logo.png`, with its 4-way 1px `drop-shadow` outline hack — drop-shadow traces the alpha edge, box-shadow would draw a rectangle) is back in the nav. `LogoWordmark.tsx`, its caret keyframes in globals.css, and the Link's `min-w` hit-area reservation were deleted outright (YAGNI — it lives in git history if ever wanted). Files: `src/lib/comparisonPool.ts` (new), `src/app/api/rankings/route.ts`, `src/app/api/comparison/next/route.ts`, `src/app/rankings/page.tsx`, `src/components/NavBar.tsx`, `src/app/globals.css`, `src/components/LogoWordmark.tsx` (deleted).

- [x] (**UI polish pass — Apple-like minimalism**) Seven changes on the `ui-polish` branch, one commit each. (1) FONT: the app-wide Source Code Pro (monospace, read as dev tooling) was replaced by the platform system sans — `--font-sans: system-ui, -apple-system, …` in globals.css, no web font downloaded; SF Pro renders on Apple devices. The Bitcount pixel face stays on the rating numbers as the single playful accent. (2) LOGO: the raster PNG (with its 4-way drop-shadow outline hack) became `LogoWordmark`, an animated typed wordmark — types "PokeMash" with a blinking `steps()` caret, pauses, backspaces to one randomly chosen letter, retypes it in the FLIPPED brand color (base scheme "Poke" dark / "Mash" red, so the accent letter is red-in-Poke or dark-in-Mash), then the caret fades; different letter every load. All keystrokes preschedule as one flat setTimeout list (trivially clearable in cleanup, safe under dev double-effects); the accent index rides INSIDE each frame (one state, satisfies react-hooks/set-state-in-effect); randomness lives in the effect so SSR/hydration both render the empty frame; prefers-reduced-motion gets the finished name statically. Nav `<Link>` keeps `aria-label="PokeMash"`, the churn is aria-hidden; min-w on the Link stops the hit area resizing while typing. (3) STREAK LEGEND: shown only while a glow is actually on the board — `legendVisible = flameColor(streak) !== null` (same tier walk as the card flame), threaded to PanelLeft and the mobile block; the legend stays MOUNTED and fades via opacity + aria-hidden so appearing never reflows. (4) MOBILE RHYTHM: two causes of the low/lopsided phone board — the mobile legend reserved a ~90px flex band even when invisible (now a `pointer-events-none absolute left-4 top-16 z-20` overlay), and `pb-28` inside the justify-center column shifted the group up by half the padding (now `pb-10`, just the undo button's translate hang; desktop `md:pb-40` untouched). (5) CLEFAIRY vs RATING TEXT: she strolled over the numbers because `RatingDial` is `absolute top-full` — OUT of flow — so the card wrapper rect her `roamBottom` no-go zones were built from stopped at the card's bottom edge (the old comment claimed the wrapper included the dial; it never did). `roamBottom` now extends `DIAL_BAND_H` (52px ≈ mt-3 + text-3xl line) past the wrapper; this also seats the serpents' swim lane (`lowest = max(roamBottom)`) genuinely below the dials as always intended. (6) SKELETONS: cold loads showed bare white slots with orphaned `i` buttons; each slot now pulses a card-shaped `absolute inset-0` placeholder (the img's width/height attrs reserve the aspect box pre-bytes), art fades in on `onLoad` (tracked per-URL in `loadedArt`), and the info button renders only once art exists. Preloaded art is browser-cached so onLoad fires at mount — no skeleton flash on warm swaps. (7) ACCENT: CardBack's Buy button blue-600 → red-600 (one accent color everywhere; single-file change thanks to the earlier CardBack extraction), plus the inert `Login` placeholder (title="Coming soon") removed from the nav — dead UI reads as broken. Verified in installed Chrome desktop+mobile viewports: legend hidden at rest / fading in at a real 5-streak, balanced mobile centering, skeleton under CDP network throttling, red Buy on the flip, rankings regression. Pre-existing lint error in Clefairy.tsx (set-state-in-effect at the mount reset) exists on main and was left alone. REVISED after user review: (1) the system sans read too plain — the app font is now **Pixelify Sans** (next/font again; a pixel face WITH true lowercase, so it keeps the retro-Nintendo feel of the Source Code Pro era without body text going illegible; the deciding property vs Press Start 2P/VT323); (2) the wordmark's random-letter flip became a deterministic vignette — type all black → erase "Mash" → retype it red (the old logo's split) — replayed from a blank line every 30s via `play()` + `setInterval(play, CYCLE_MS)`, script hoisted to module scope since nothing is random anymore; (3) the Login placeholder pill was RESTORED at the user's request (reserves the top-right spot for the real login flow); (4) the legend went from all-at-once to tier-by-tier: `StreakLegend` now takes the raw `streak` and each row slides in (`-translate-x-2 opacity-0` → settled) as the streak reaches its threshold, all vanishing on a break — the legend narrates the run instead of keying it. Files: `src/app/globals.css`, `src/app/layout.tsx`, `src/components/LogoWordmark.tsx` (new), `src/components/NavBar.tsx`, `src/components/StreakLegend.tsx`, `src/components/PanelLeft.tsx`, `src/components/CardBack.tsx`, `src/app/compare/ComparisonScreen.tsx`, `src/app/compare/ComparisonArea.tsx`, `src/app/compare/Clefairy.tsx`.

- [x] (**card info in the matchup**) Let players learn about a card during the head-to-head without choosing it — name, pack, set, release date, and a Buy Now link. Clicking a card means picking it, so "view info" needed a spatially separate gesture: a small translucent `i` in each card's top-right corner (faint, brightening on card hover) flips the card over (rotateY, same pattern as the Rankings flip) to an info back with a Name/Pack/Set/Released table, a Buy on TCGplayer button, and the FTC affiliate disclosure. While flipped a card cannot be picked — clicking anywhere on it flips it back to the front — and picking either card closes any open back; the opponent stays face-up and pickable throughout. At most one card is flipped at a time (`flippedId` state, local to `ComparisonArea` since it's purely cosmetic). The pick target became a `role="button"` div (the `RankingCard` pattern) so the info button and the back's referral `<a>` aren't invalid nested interactives; the rotateY lives on an inner element so it doesn't fight the outer slide/hover transforms, sitting at z-10 between the streak-glow backing (z-0) and the pick flash (z-20). DRY: the rankings back face was extracted into shared `CardBack` (details table + referral button + disclosure in one place, with text/padding tightening below `md` so it fits phone-width 44vw cards) and the `orDash`/`packName`/`tcgplayerSearchUrl` helpers moved to `lib/cardInfo.ts`. `/api/comparison/next` now sends `set`/`pack`/`release_date` per card (already fetched for pool eligibility, previously stripped; also added to the held-winner fallback fetch); the client `Card` fields are optional so pairs restored from older sessionStorage saves render an em dash. FIX (Pikachu 160/159 showed dashes despite a fully populated DB row): a held winner restored from a pre-change save never heals — the client keeps its own winner object across the whole streak (Keep Winner responses only contribute `next.slice(1)` challengers; picks fold in rating fields only) — so the saved-pair key is now versioned (`pokemash:comparison` → `pokemash:comparison:v2`), discarding pre-change saves once; bump the suffix on any future saved-Card shape change. Verified end-to-end in the user's installed Chrome (flip → details/Buy href, unflip-not-pick, pick-while-other-flipped, phone-width fit, rankings regression); NOTE: Playwright's WebKit port paints rotated-away back faces (`backface-visibility` culling never happens there, even for a textbook minimal flip — the shipped Rankings flip renders the same way in it), so that browser can't be used to judge this pattern; worth one manual look in real Safari someday. Files: `src/components/CardBack.tsx`, `src/lib/cardInfo.ts`, `src/app/compare/ComparisonArea.tsx`, `src/app/api/comparison/next/route.ts`, `src/app/rankings/page.tsx`.

- [x] (**pre-loaded future comparisons**) Original spec — *PROBLEM*: Card selection takes too long. *SOLUTION*: Quicken card selection process by preloading the possible future cards so that when the player makes a comparison, the next card(s) to load-in is already present in memory. This function must work for both selections of the `Keep Winner` toggle. Implemented in stages; final form is a batched challenger QUEUE. Why one-deep preload wasn't enough: every pick restarted a ~1–1.5s chain (two speculative `/api/comparison/next` calls joined by `Promise.all` at ~0.5–0.65s each, then the image warm at ~0.2–0.85s), which a sub-2-second click races and often beats — and the old `warmImage` warmed the RAW pkmncards.com URL while the mounted `next/image` requests the `/_next/image?url=…&w=…&q=…` optimizer URL, so even "warmed" art loaded cold at mount (fixed first, via `getImageProps` + shared `CARD_IMAGE` dims, mirroring srcSet/sizes so the browser selects the identical candidate). Queue design: the API takes `count` (capped `FRESH_COUNT_MAX` 5) and a comma-separated `excludeId`, returning `[winner, ...N challengers]` from `supply_winner_with_fresh_cards` — each draw uses the same explore/exploit policy with picked cards removed from the pool between draws, and since the route's cost (count/ranks/history + sample) is per-request, N challengers cost the same as one. Client (`ComparisonScreen`): `preloadRef` in Keep Winner mode holds `{filterKey, queues: Record<winnerId, Card[]>}` topped up to `QUEUE_DEPTH` 3 per on-board side; each side's fetch stores independently on resolve (no `Promise.all` coupling — a click between the two still hits whichever landed), validated by store identity + winner-still-on-board, deduped against board and queue, with an in-flight set so re-firing effects can't stack requests. A pick first recycles the loser's leftover queue into the winner's `donated` stopgaps instead of deleting it, THEN pops the winner's best available challenger (`popChallenger`: decoded art preferred, fresh entries before donated) — donation must precede the pop, because on a winner switch the new winner's own queue is still fetching and this pick's challenger has to come from the hand-me-downs (donating after the pop only ever helps the following pick, leaving switches slow). Donated cards were rating-matched to the beaten opponent but are already fetched/warmed, which makes switching winners as fast as a streak pick; they don't count toward `QUEUE_DEPTH`, so fresh winner-matched entries rebuild underneath and supersede them. The surviving queue persists across the winner's whole streak — refills are `count = 3 - len`, usually 1. Images: `imageWarmth` map tracks decode state via `img.decode()`; only the front `WARM_DEPTH` 2 per queue are warmed (art for the losing side is often discarded); the overlap fast paths in BOTH toggle modes now require `imageReady` — data-hit-but-cold-art degrades to the sequential slide, whose 350ms buys the download time, so a card never mounts with visibly-loading art. Filter changes null the store; mode/filter mismatch rebuilds it (in-flight resolves compare `preloadRef.current !== store` and drop themselves). Accepted tradeoffs (user-approved): queue-tail matchups are chosen against a slightly stale rating (imperceptible at depth 3; epsilon policy is identical for queued vs fresh draws), and a filter/toggle flip pays one cold fetch. Files: `src/app/api/comparison/next/route.ts`, `src/app/compare/ComparisonScreen.tsx`, `src/app/compare/ComparisonArea.tsx`.

- [x] (**filter by price / era / series**) New Filter button on the left of the Play toolbar (opposite Keep Winner) opening `FilterModal` with three sections: a price range (min/max on `market_price` — each card's recent-sale-based market value, in USD; labeled as such in the modal), an era multi-select (Vintage 1999–2007 / Middle 2008–2016 / Modern 2017+), and a searchable series multi-select. "Series" mostly maps to the `cards.set` column (era values like Sword & Shield, Scarlet & Violet, Sun & Moon), with a few curated entries: "Original" is the WOTC `Classic` set under a friendlier label, and "Legendary Collection" is a `pack` (carved out of the `Other` set) — `matchesSeries` matches it by `pack` and excludes it from the `Other` option so the two are disjoint. (`Misc.` was dropped; `Collections` is the 2019 Shiny Vault pack.) `/api/comparison/next` accepts `minPrice`/`maxPrice`/`eras`/`series`: series and price are pushed into the Supabase query; era uses a DB `set IN (ERA_SETS)` pre-filter (so the contiguous random sample window is era-relevant) plus a precise JS release-year check (`matchesEras`) to trim boundary-spanning series (Promos span 1999–2026). Because a restrictive filter can leave a window with <2 eligible cards, the sample is retried with fresh offsets up to `SAMPLE_RETRIES` times before returning 409; the client shows a "No cards match these filters" message instead of crashing. Filters live in `ComparisonScreen` state + a `filtersRef` so the async fetches read current values, are serialized by `buildFilterQuery`, and applying re-fetches immediately. A dot badges the Filter button when any filter is active. Price column choice: all three price columns (`lowest_price`/`market_price`/`highest_price`) share identical non-null coverage (filled together; ~10% of cards have none), so coverage can't be the tiebreaker. `lowest`/`highest` are the cheapest/priciest current LISTINGS (asking prices — in 99% of rows `lowest <= market <= highest`), while `market_price` is the value from recent actual SALES — the closest thing to a "last sold" price and the most meaningful basis, so the filter uses it. (`highest_price` was tried first because it surfaces more cards over a threshold, but that's just the top asking price, not what cards sell for.) The `0` sentinel market_price uses for "no sales data" is excluded as junk. There is no literal last-sold column; market_price is the proxy. Known limits: the series list is static (no `distinct_sets` RPC), `~10%` of cards have no price at all (excluded when a price bound is set), and filters reset on navigation. Files: `src/components/FilterModal.tsx`, `src/components/FilterButton.tsx`, `src/app/compare/ComparisonScreen.tsx`, `src/app/api/comparison/next/route.ts`.

- [x] (**no self-matchups**) A card must never be compared against itself. Both pair-builders in `/api/comparison/next` already exclude the first/winner card by `card_id` (`information_rich_pair` filters out `cardA`; `supply_winner_with_fresh_card` filters out the `winner`), so a literal id self-pair couldn't occur — but there was no guard against the source data carrying the same card under duplicate rows. Added a pool-level de-dup: the eligible rows are collected into a `Map` keyed by `card_id` (`eligibleById`, first row per id wins) before pairing, so each card appears at most once and the two selected entries are always distinct cards. Files: `src/app/api/comparison/next/route.ts`.

- [x] (**consistent card comparison**) A loaded comparison pair shouldn't disappear when the user leaves Play and comes back (e.g. to Rankings and back). Implemented in `ComparisonScreen`: the settled pair plus its streak (`{cards, streak, streakCardId}`) is written to `sessionStorage` under `pokemash:comparison` in an effect gated on `ready` (so only a fully-centered board is saved, never the transient `null`/mid-slide states). On mount, `readSavedComparison` restores that pair directly at center and `ready`, returning before the usual `loadNextPair` fetch; with nothing saved it falls back to fetching a fresh pair. sessionStorage (not localStorage) because this is a transient, per-tab concern, not durable progress. Restore runs in `useEffect` (not a `useState` lazy initializer) to avoid a hydration mismatch, since the component is server-rendered for the initial HTML. Files: `src/app/compare/ComparisonScreen.tsx`.

- [x] (**streak flame — make it a glow**) Winning-streak indicator (Keep Winner mode, tiers at 5/10/20 wins → dark red / blue / purple) on the held winner's card. Several attempts to render an actual flame were all rejected as ugly: (1) flickering box-shadow glow; (2) box-shadow ruffled by an SVG turbulence/displacement filter (`#flame-distort`); (3) teardrop `.flame-tongue` outlines rippled by that filter + opacity flicker; (4) outward-pointing SVG tongues rising/swaying via `scale`/`rotate` with a tip-fade mask + `drop-shadow` halo; (5) a full ring of upward-pointing SVG flame tongues (tall top crown, side climbers, bottom risers) filled by baked per-tier gradients, animating only `transform`/`opacity` with a static blur. FINAL (this entry): scrapped the flame entirely and went with a colored glow per the user's reference. `.flame` is a layer sitting just behind the card (`z-0`, `inset: -3px` so a thin colored backing shows at the border) with a solid `background: rgb(var(--flame-color))` fill plus a two-layer `box-shadow` halo glowing outward; only `opacity` animates (`flame-pulse`, compositor-cheap, keeps the edge from shifting). `--flame-color` is the tier triple from `flameColor(streak)`, which walks `STREAK_TIERS` (now four tiers: 5 red / 10 orange / 20 blue / 40 violet) and returns the highest reached. A minimal **legend** (`<ul>` of color-dot + `N+` rows, also driven by `STREAK_TIERS` as the single source) is pinned to the left edge, centered on the 25%-from-top line (75% up the card area). Removed all the tongue markup, gradient `<defs>`, `FLAME_TONGUES`/`FLAME_PATH`/`FLAME_GRADIENTS`, and every old flame keyframe/class. Streak-tracking logic (`streak`/`streakCardId`) unchanged. Files: `src/app/compare/ComparisonScreen.tsx`, `src/app/globals.css`.

- [x] (**rarity-restricted comparison pool**) Stop comparing boring cards by restricting the pool by rarity, with era-aware rules. Implemented in `/api/comparison/next` (app code, using the read-only key): exclude `Common`/`Uncommon`/`No Rarity`/`Double Rare`; keep plain `Rare` only for vintage sets (release year < 2023); keep all other rarities (holos, ex/GX, V/VMAX/VSTAR, Illustration/Special Illustration/Ultra/Secret/Hyper, Promo, Trainer Gallery, Mega, etc.). The route excludes the always-drop rarities server-side via a `not in` filter, samples a random ~1000-card window (to get variety past PostgREST's row cap), then drops modern plain `Rare` in JS because `release_date` is free text the DB filter can't compare by year. The user-chosen manual `Filter` button was removed beforehand. Equivalent SQL lives in `supabase/migrations/20260630_comparison_pool.sql` for a future move into the DB (tracked as an open TODO). Per-era mechanic rares are kept (regular GX in Sun & Moon, V/VMAX/VSTAR in Sword & Shield, EX in XY) — but not the regular ex of Scarlet & Violet / Mega Evolution, which is the already-dropped `Double Rare`. Energy cards are also excluded: matched by name (last word "Energy" after stripping `{symbols}`/"Prism Star"), which spares trainers like "Energy Retrieval"; this catches ~114 full-art/secret energies (Ultra Rare/Hyper Rare/ACE SPEC) that carry otherwise-kept rarities. Promos and plain `Rare` are both catch-all rarities (boring non-holos mixed with the odd buzzword chase card), so they're judged by card mechanic via `hasFeaturedMechanic` (name ends in GX/V/VMAX/VSTAR/ex/EX/LV.X/BREAK/Prime/LEGEND/Star) rather than rarity. A `Promo` is kept only with a mechanic; a `Rare` or `Rare Holo` is kept with a mechanic OR if genuinely vintage — released before Black & White (2011-03-01, i.e. HeartGold & SoulSilver and earlier), compared as a full date (`new Date(...)`) since the HGSS→BW boundary falls inside 2011. (`Rare Holo` follows the same vintage rule because a modern `Rare Holo` is a plain foil, e.g. Scizor 126/214 from 2018, while a vintage one is a chase card, e.g. Base Set Charizard.) This is safe because full-art cards always carry a distinct rarity (e.g. Reshiram 113/114 is `Ultra Rare`), so plain `Rare`/`Promo` never holds a full art. Net pool ~7688 eligible; drops modern non-buzzword `Rare` (e.g. Gardevoir 069/195, Silver Tempest 2022) while keeping vintage `Rare` and buzzword `Rare` (Deoxys ex, Umbreon Star).

- [x] (**exclude Item/Stadium trainers**) Trainer cards with subtype Item or Stadium are removed from the pool. The `cards` table has no card-type column, so they're matched by name against `src/lib/itemStadiumNames.ts` — a 573-name list (368 Item + 205 Stadium) generated from the Pokémon TCG API (`supertype:Trainer subtypes:Item/Stadium`). `normalizeName` in `api/comparison/next` (NFKD → drop non-ASCII → lowercase → punctuation-to-space → collapse) matches the list's normalization, so apostrophe/accent differences between sources don't matter. Supporters and Tools are kept. Removes the ~191 full-art/secret Items/Stadiums that were in the eligible pool (pool ~6885 → ~6694). The list is static and should be regenerated when new sets add Items/Stadiums.

- [x] (**add filters to comparison pool**) On left side of the `Play` screen, on the same level as the `Keep Winner` toggle, add a `Filter` button. Support the ability to filter by field names from the database. To start, only implement filtering on `cards.rarity`. When the user clicks the `Filter` button, a modal should popup that includes a search box titled `Rarity` above it. When the user clicks on the search box, the different values for `cards.rarity` column populate a scrollable dropdown menu. When the user types into the search box, only those values that contain the inputed string (when both are brought to lowercase) remain. There is a checkbox next to each of the values. When the user selects one of those values, it stays highlighted, and a small box with the name of the applied filter pops up next to the search box. In the code, this area will be called the `applied_filter area`. In this modal, there are three options to exit. First, the user can click the `Apply` button, which applies the filters and ensures that the next set of cards meets the filter criteria (that will required updating the `comparison/next` GET function). Next, there will be an `x` in the upper right corner that, when clicked, exits out without applying the filters. Or, the user can click anywhere outside of the modal, and the modal will close without applying filters (same function as hitting `x`).